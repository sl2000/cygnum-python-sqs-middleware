sudo systemctl restart nginx
sudo systemctl kill -s HUP cygnum.service -- restart gunicorn gracefully with new config / software - see http://docs.gunicorn.org/en/latest/signals.html

sudo less /var/log/nginx/error.log -- checks the Nginx error logs.
sudo less /var/log/nginx/access.log -- checks the Nginx access logs.
sudo journalctl -u nginx -- checks the Nginx process logs.
sudo journalctl -u cygnum --since "1 hour ago" -- checks app log

source env/bin/activate
gunicorn --config gunicorn.conf.py wsgi:app - run directly

source env/bin/activate
pip freeze > requirements.txt

git status
git diff [file]
git commit -a -m "description of change"
git push origin
git fetch origin
git pull
