nginx -t -- test nginx configuration
systemctl reload nginx -- restart nginx
systemctl kill -s HUP cygnum-xxx -- restart gunicorn gracefully with new config / software - see http://docs.gunicorn.org/en/latest/signals.html

less /var/log/nginx/error.log -- view the Nginx error logs.
less /var/log/nginx/access.log -- view the Nginx access logs.
journalctl -u nginx -- view the Nginx process logs.
journalctl -u cygnum-xxx --since "1 hour ago" -- view Python app log (e.g. cygnum-dev, cygnum-c41 etc)

source env/bin/activate
gunicorn --config gunicorn.conf.py wsgi:app -- run directly (may want to use when developing)

source env/bin/activate
pip freeze > requirements.txt
deactivate

git status
git diff [file]
git commit -a -m "description of change"
git push origin
git fetch origin
git pull
